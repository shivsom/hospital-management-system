# ─────────────────────────────────────────────────────────────
# HMS AI Microservice — Python dependencies (pinned for reproducibility)
# Regenerate with: pip-compile requirements.in
# ─────────────────────────────────────────────────────────────

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# ML / NLP
transformers==4.36.0
# Use CPU-only torch to keep the image ~2 GB smaller.
# Replace with torch==2.1.1+cu118 if you need GPU support.
torch==2.1.1 --index-url https://download.pytorch.org/whl/cpu
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.25.2

# Multipart form support
python-multipart==0.0.6
