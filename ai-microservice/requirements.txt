# 
# HMS AI Microservice — Python dependencies
#
# IMPORTANT: torch must be installed with the CPU-only index.
# The Dockerfile passes --extra-index-url so this file stays clean.
# Do NOT add --index-url inline here — pip silently ignores it
# when used with --prefix in some versions.
# 

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# ML / NLP — CPU-only torch (~700 MB vs ~2.5 GB for CUDA build)
torch==2.1.1
transformers==4.36.0
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.25.2

# Multipart form support
python-multipart==0.0.6
